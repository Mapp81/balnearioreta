<div class="container-link-card">
  <div class="link-card">
    <a href="/Playas">
      <div class="card-content">
        <img src="../src/assets/Lapausa2.png" alt="Parador de mar" class="card-img">
        <div class="overlay">
          <h3>Playas</h3>
        </div>
      </div>
    </a>
  </div>

  <div class="link-card">
    <a href="/Alojamiento">
      <div class="card-content">
        <img src="../src/assets/fotoavifterrazas.jpg" alt="Lugares para alojarse en Reta" class="card-img">
        <div class="overlay">
          <h3>Alojamiento</h3>
        </div>
      </div>
    </a>
  </div>

  <div class="link-card">
    <a href="/Actividades">
      <div class="card-content">
        <img src="../src/assets/pescadorescorvina.jpg" alt="Disfrutá las variadas actividades que ofrece Reta" class="card-img">
        <div class="overlay">
          <h3>Actividades</h3>
        </div>
      </div>
    </a>
  </div>
</div>

<style>
/* ✅ Contenedor principal */
.container-link-card {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 20px;
  margin-bottom: 8%;
}

/* ✅ Cada card */
.link-card {
  border-radius: 8px;
  overflow: hidden;
  background-color: white;
  box-shadow: 0 4px 8px var(--base);
  transition: transform 0.3s ease-in-out, box-shadow 0.3s ease-in-out;
}

/* 🎯 SOLUCIÓN: Evita el temblor al hacer hover */
.link-card:hover {
  transform: scale(1.02);
  box-shadow: 0 6px 12px var(--gris);
}

/* ✅ Enlace que cubre toda la card */
.link-card a {
  display: block;
  text-decoration: none;
  color: inherit;
}

/* ✅ Contenedor interno para evitar problemas de tamaño */
.card-content {
  position: relative;
  width: 300px;
  height: 350px;
}

/* ✅ Imagen dentro de la card */
.card-img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}

/* ✅ Overlay (efecto al pasar el mouse) */
.overlay {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.4);
  color: white;
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
  opacity: 0;
  transition: opacity 0.3s ease-in-out;
  font-family: "roboto", sans-serif;
  letter-spacing: 5px;
  font-weight: 300;
}

/* 🎯 Overlay aparece al hacer hover */
.link-card:hover .overlay {
  opacity: 1;
}
</style>
